console.log("arr1 ", [1, 2, 3]);

console.log("items dentro do array", [1, 2, 3, 4, 5].length);

console.log(
  [1, 2, 3, 4, 5].forEach((res, index) => {
    console.log(`valor ${res}, indice ${index}`);
  })
);
