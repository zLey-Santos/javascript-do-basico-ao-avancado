// Ordem de precedência

console.log((5 * 2) / 5 === 2); // true.
// Pela ordem de precedência resolvemos primeiro o que está entre parenteses (primeiro a ser resolvido ) depois o que está por fora da questao.

console.log((50 / 4) * 10 === 120 && 2 * 2 === 4); // false pois 12.5 * 10 = 125 como apenas um lado corresponde ao valor true e outro o valor false o resultado é false
