// Remainder ele calcula o resto de. usamos o simbilo de porcentual (%)

console.log(4 % 2); // retorna 0 (porque ele nao teve um resto)
console.log(5 % 2); //ele retorna 1 sendo assim o restante 1 a sobra / o resto

// %(remainder ou resto ) serve para verificar os valores restante se ouve ou não dentro do calculo de divisão
// podemos verificar no caso se o número é impár ou par se ele retorna 0 par se ele for maior que 0 retorna 1 impár

// veremos sobre maior e menos mais a frente..
